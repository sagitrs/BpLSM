namespace sagitrs {
/*
void BFileVecIterator::SubIterateCount(
 BVersion* bversion, uint64_t id, uint64_t time) {
  auto p = handles_.find(id);
  assert(p != handles_.end());
  bversion->
  p->second->file_->UpdateStatistics(KSIterateCount, 1, time);
}
*/
}